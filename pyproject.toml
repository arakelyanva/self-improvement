[project]
name = "self-improvement"
version = "0.1.0"
description = "Testing automation and deploy on self-hosted runner"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "google-genai>=2.2.0",
    "pygithub>=2.9.1",
    "python-dotenv>=1.2.2",
]
